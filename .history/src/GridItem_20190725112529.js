import React, { Component } from 'react'
import { Text, View, TouchableWithoutFeedback } from 'react-native'

const Item = ({item, onPhotoOpen}) => 
    <TouchableWithoutFeedback onPress={() => onP}>

    </TouchableWithoutFeedback>
